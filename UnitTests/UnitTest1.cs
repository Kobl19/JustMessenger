using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using BusinessLogic.Interfaces;
using WebUI.Models;
using WebUI.Controllers;
using System.Web.Mvc;
using System.Threading.Tasks;

namespace UnitTests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void Edit_ChangeClient_Client()
        {
            //Mock<IUserService> mock = new Mock<IUserService>();
            //AccountController controller = new AccountController();
            //EditViewModel editViewModel = new EditViewModel() {FirstName="Denis123", LastName="Kobl123", Status="В сети" };

            //Task<ActionResult> actionResult=controller.Edit(editViewModel);
            
        }
    }
}
